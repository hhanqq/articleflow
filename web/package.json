{
  "name": "articleflow-web",
  "private": true,
  "type": "module",
  "scripts": {
    "e2e": "playwright test",
    "test": "node --test tests/*.test.mjs",
    "start": "python3 -m http.server 5173 --directory ."
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
